[project]
name = "dhqwords"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "neuxml>=1.0.0",
    "pyyaml>=6.0.2",
]

[tool.uv]
package = false